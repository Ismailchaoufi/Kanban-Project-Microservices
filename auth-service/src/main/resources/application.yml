##deployement
#spring:
#  config:
#    import: optional:configserver:http://config-server:8888
#  application:
#    name: auth-service

#local
spring:
  config:
    import: configserver:http://localhost:8888
  application:
    name: auth-service